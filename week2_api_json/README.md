🎯第2周学习重点（只抓4件事）
JSON = Python 的 dict/list（字典/列表）
HTTP请求 = 用 requests.get/post 拿到数据
状态码与异常处理：200 成功；非200要报错/重试
把外部API包装成“工具函数”（第4周Tool Calling就直接用）

🧪 第2周必须完成的练习题（按步骤）
练习1：把返回JSON里“你关心的字段”抽出来（5分钟）
在 fetch_repo() 里多打印一个字段：default_branch。
要求：不改结构，只加一行打印。

练习2：做一个“停车场事件API”模拟（30分钟）
在 main.py 里新增一个字典：
event = {"event": "IllegalParking", "target": "A-023", "ts": "..."}
然后把它 json.dumps() 成字符串打印出来。
目的：让你习惯“事件=结构化数据”，后续对接状态机/调度。

练习3：错误处理（20分钟）
把 fetch_repo("psf", "requests") 改成一个不存在的 repo 名，观察错误。
要求：你能回答两个问题：
报错里显示的 HTTP 状态码是多少？
你觉得这种错误在生产系统该怎么处理（重试/告警/降级）？

✅你做完第2周后，应该具备的能力
能把任何外部系统封装成 get_xxx() 工具函数
能用 JSON 组织“事件/任务/结果”数据
能处理错误与限流（这是RaaS线上系统的基本功）
这会直接对应你们“事件驱动 + 任务编排 + 闭环”路径（战略文档已明确这一点）